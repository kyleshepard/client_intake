import React, { useState } from "react";
import { Button } from "@material-ui/core";

export const FormPage = () => {
    ///This page is currently lame, but it should route correctly hopefully maybe
    ///Add Element Button
        ///File upload, textbox, checkbox
            ///Remove button attached to each subfield
    return (
        <div>
            <Button>Add element</Button>
        </div>
    )
}